import { Platform } from 'react-native';

export const IS_ANDROID = Platform.OS === 'android';
export const IS_IOS = Platform.OS === 'ios';
export const FALLBACK_IMAGE_URI = 'https://starwars-visualguide.com/assets/img/big-placeholder.jpg';
export const SOUND_SPEED = 1235;
